# Tools

- **Runtime JS** : Bun. Packages npm : pnpm. Node versions : NVM.
- **Rust** : en apprentissage — **ne pas présenter comme maîtrisé** (CV, candidatures, profils).
- **Mobile** : Capacitor. **Allergique à React Native**.
- **Delete** : `trash` / `gio trash`, jamais `rm -rf` (hook enforce).
- **Editor** : nvim. **Shell** : ZSH.
- **Aliases** : `cc` = CC skip perms · `ccc` = skip perms + continue.

## Waybar gotchas
- JSON : `jq -cn` (compact), jamais `jq -n`.
- Edit/Write drop Nerd Font PUA → injecter via `sed` + `printf`.
- Pango tooltip : uniquement modules avec `return-type: json`.
