[
  {
    "context": "Editor && mode == full",
    "bindings": {
      "ctrl-enter": "assistant::InlineAssist"
    }
  },
  {
    "context": "Editor && extension == md",
    "bindings": {
      "ctrl-shift-m": ["task::Spawn", { "task_name": "Markdown preview (grip)" }]
    }
  }
]
